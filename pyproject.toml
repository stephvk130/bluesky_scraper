[project]
name = "bluesky-canada-scraper"
version = "0.1.0"
description = "Daily scraper for BlueSky posts mentioning Canada"
requires-python = ">=3.12"
dependencies = [
    "atproto>=0.0.55",
    "pandas>=2.2.0",
]
